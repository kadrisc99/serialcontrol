port = "COM4";   // Need to change

command = "F100"
commandTerminator = "\r";
answerTermiantor = "\n"
mmc.setSerialPortCommand(port, command, commandTerminator);

answer = mmc.getSerialPortAnswer(port, answerTerminator);

command = "D050"
commandTerminator = "\r";
answerTermiantor = "\n"
mmc.setSerialPortCommand(port, command, commandTerminator);

answer = mmc.getSerialPortAnswer(port, answerTerminator);
